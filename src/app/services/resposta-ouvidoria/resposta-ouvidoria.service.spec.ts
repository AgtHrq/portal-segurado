/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { RespostaOuvidoriaService } from './resposta-ouvidoria.service';

describe('Service: RespostaOuvidoria', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [RespostaOuvidoriaService]
    });
  });

  it('should ...', inject([RespostaOuvidoriaService], (service: RespostaOuvidoriaService) => {
    expect(service).toBeTruthy();
  }));
});

import { IdVinculoPipe } from './id-vinculo.pipe';

describe('IdVinculoPipe', () => {
  it('create an instance', () => {
    const pipe = new IdVinculoPipe();
    expect(pipe).toBeTruthy();
  });
});

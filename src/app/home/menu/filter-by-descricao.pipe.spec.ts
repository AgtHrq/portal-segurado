import { FilterByDescricaoPipe } from './filter-by-descricao.pipe';

describe('FilterByDescricaoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByDescricaoPipe();
    expect(pipe).toBeTruthy();
  });
});
